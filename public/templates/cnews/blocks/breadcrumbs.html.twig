<div class="">
	<div class="breadcrumbs_menu">
		{% for page in rootline %} <a href="{{url({'pageId':page.id})}}"> {{ page.text }}
		</a> &nbsp;/&nbsp; {% endfor %}
        <span class="current">{{ currentPage.text }}</span>
	</div>
</div>
