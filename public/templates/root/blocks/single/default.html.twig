{% extends "root/block.html.twig" %}

{% block mainContent %} 

	<div class="block headline">
	<h1 class="editable" id="{{ data.id }}_text">{{ data.text |cleanHtml }}</h1>
	<div class="editable" id="{{ data.id }}_summary">
			{{ data.summary|cleanHtml }}
		</div>
	 {% for key, value in data %}
	        {% if value %}
      		{%for cKey,cValue in type %}
      			{%if(key==cKey)%}
      					{%if cValue=="ImagePickerField"%}
      					    <div>
      					    <span class="label">{{key}} : </span>
      						<img alt=""  class="imageLeft" src="/dam?media-id={{value}}" />
      						</div>
      					{%elseif(cValue=="CKEField")%}
      					<div><span class="label">{{key}} : </span><div class="entry-content editable"  id="{{ data.id }}_{{key}}">
						{{value|cleanHtml}}
						</div></div>
						{%elseif(cValue=="datefield")%}
						<div><span class="label">{{key}} : </span><span class="label label-info" id="{{ data.id }}_{{key}}">{{ value }}</span></div>
      					{%elseif(cValue=="textfield")%}
      					<div><span class="label">{{key}} : </span><h3 class="editable" id="{{ data.id }}_{{key}}">{{ value |cleanHtml }}  </h3></div>
      					{%elseif(cValue=="numberfield")%}
      					<div><span class="label">{{key}} : </span><input type="number" name="quantity" value="{{ value |cleanHtml }}"></div>
      					{%elseif(cValue=="colorfield")%}
      					<div><span class="label">{{key}} : </span><input type="color" id="{{ data.id }}_{{key}}" name="favcolor" value="{{ value |cleanHtml }}"></div>
      					{%elseif(cValue=="slider")%}
      					<div><span class="label">{{key}} : </span><input type="range" id="{{ data.id }}_{{key}}" value="{{ value |cleanHtml }}"></div>
      					{%elseif(cValue=="timefield")%}
      					<div><spa class="label"n>{{key}} : </span><h3 class="editable" id="{{ data.id }}_{{key}}">{{ value |cleanHtml }} </h3></div>
      					{%elseif(cValue=="ratingField")%}
      					<div><span class="label">{{key}} : </span>
      							{%for i in 0..value%}
      							<i class="icon-star"></i>
      							{%endfor%}
      						</div>
      					
      					{%endif%}
      			{%endif%}
      		{%endfor%}
      		{%endif%}
    {% endfor %}
</div>
{% endblock %}
