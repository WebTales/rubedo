<ul class="breadcrumb">
	{% for lien in liens %}
	<li {% if lien.current %}class="active"{% endif %}>
		<a href="/{{ lien.controller }}">{{ lien.libelle }}</a>
		{% if loop.index < loop.length %}<span class="divider">/</span>{% endif %}
	</li>
	{% endfor %}
</ul>
