 {% if(cValue["config"]["multivalued"])and value is iterable %} {% for item in value %}
<div>
	<a href="{{url({pageId: item},true)}}">{{getPageTitle(item)}}</a>
</div>
{% endfor %} {% else %}
<div>
	<a href="{{url({pageId: value},true)}}">{{getPageTitle(value)}}</a>
</div>
{% endif %}
