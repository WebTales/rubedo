{% extends "/block.html.twig" %}
{% block mainContent %}
    <div id="shoppingcart">
        <div class="navbar">
            <div class="navbar-inner">
                <a class="brand">Shopping Cart <span class="badge spcnboitems">{{ totalItems }}</span></a>
            </div>
        </div>
        <div class="shoppingcartholder details">
            {% include '/blocks/shoppingCart/productList.html.twig' %}
        </div>
        <div>
            {% if checkoutPage %}<a href="{{ checkoutPageUrl }}"><button class="btn pull-right scartbtn">Checkout</button></a>{% endif %}
        </div>
    </div>

{% endblock %}